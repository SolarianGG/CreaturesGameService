# Copy to .env (git-ignored) before `docker compose up`; compose.yaml has no defaults (D-196).
# Local development values; the local Spring profile uses the same ones (D-200, D-204).
POSTGRES_DB=gameservice
POSTGRES_USER=gameservice
POSTGRES_PASSWORD=gameservice
RABBITMQ_DEFAULT_USER=gameservice
RABBITMQ_DEFAULT_PASS=gameservice
